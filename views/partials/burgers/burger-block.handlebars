<p align="center">
	{{#if devoured}}
	
	{{id}}.
	{{name}}

	{{else}}

	{{id}}.
	{{name}}

	<button class="devour-it" data-id="{{id}}" data-newdevour="true">
		{{!-- {{#if devoured}}ALREADY EATEN{{else}}DEVOUR{{/if}} --}}
		DEVOUR
	</button>

	{{/if}}

</p>
